name: auth-sandbox

services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - keycloak-postgres-data:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${POSTGRES_USER:-keycloak}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  keycloak:
    image: quay.io/keycloak/keycloak:26.5
    restart: unless-stopped
    command:
      - start-dev
      - --proxy-headers=xforwarded
      - --hostname=https://keycloak.localhost:8443
      - --hostname-strict=false
      - --features=jwt-authorization-grant,par,step-up-authentication
    env_file: .env
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      KC_DB_USERNAME: ${POSTGRES_USER}
      KC_DB_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./keycloak-extension/build/libs:/opt/keycloak/providers:ro
    depends_on:
      postgres:
        condition: service_healthy

  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      DB_USERNAME: ${DEVICE_POSTGRES_USER}
      DB_PASSWORD: ${DEVICE_POSTGRES_PASSWORD}
      JWT_PRIVATE_KEY_PATH: /etc/auth-service/keys/private.pem
      JWT_PUBLIC_KEY_PATH: /etc/auth-service/keys/public.pem
      JWT_ISSUER: ${JWT_ISSUER}
      JWT_EXPIRATION_SECONDS: ${JWT_EXPIRATION_SECONDS:-300}
      TRANSFER_TOKEN_TTL_SECONDS: ${TRANSFER_TOKEN_TTL_SECONDS:-60}
      CHALLENGE_EXPIRATION_SECONDS: ${CHALLENGE_EXPIRATION_SECONDS:-120}
      KEYCLOAK_REALM_URL: ${KEYCLOAK_REALM_URL}
      KEYCLOAK_AUTH_ENDPOINT: ${KEYCLOAK_AUTH_ENDPOINT}
      KEYCLOAK_TOKEN_ENDPOINT: ${KEYCLOAK_TOKEN_ENDPOINT}
      KEYCLOAK_PAR_ENDPOINT: ${KEYCLOAK_PAR_ENDPOINT}
      KEYCLOAK_INTROSPECT_ENDPOINT: ${KEYCLOAK_INTROSPECT_ENDPOINT}
      KEYCLOAK_CLIENT_ID: ${KEYCLOAK_CLIENT_ID}
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET}
      KEYCLOAK_REDIRECT_URI: ${KEYCLOAK_REDIRECT_URI}
      KEYCLOAK_CALLBACK_URI: ${KEYCLOAK_CALLBACK_URI}
      KEYCLOAK_SCOPE: ${KEYCLOAK_SCOPE:-openid profile}
      KEYCLOAK_ASSERTION_EXPIRATION_SECONDS: ${KEYCLOAK_ASSERTION_EXPIRATION_SECONDS:-60}
      KEYCLOAK_ADMIN_CLIENT_ID: ${KEYCLOAK_ADMIN_CLIENT_ID}
      KEYCLOAK_ADMIN_CLIENT_SECRET: ${KEYCLOAK_ADMIN_CLIENT_SECRET}
      KEYCLOAK_ADMIN_REALM: ${KEYCLOAK_ADMIN_REALM}
      KEYCLOAK_IDP_ALIAS: ${KEYCLOAK_IDP_ALIAS}
      KEYCLOAK_SSO_PROXY_IDP_ALIAS: ${KEYCLOAK_SSO_PROXY_IDP_ALIAS}
      KEYCLOAK_ADMIN_TOKEN_ENDPOINT: ${KEYCLOAK_ADMIN_TOKEN_ENDPOINT}
      KEYCLOAK_ADMIN_BASE_URL: ${KEYCLOAK_ADMIN_BASE_URL}
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KEYCLOAK_CMS_CLIENT_ID: ${KEYCLOAK_CMS_CLIENT_ID}
      KEYCLOAK_CMS_CLIENT_SECRET: ${KEYCLOAK_CMS_CLIENT_SECRET}
      KEYCLOAK_CMS_CALLBACK_URI: ${KEYCLOAK_CMS_CALLBACK_URI:-https://cms.localhost:8443/cms/callback}
      KEYCLOAK_CMS_INTROSPECT_ENDPOINT: ${KEYCLOAK_CMS_INTROSPECT_ENDPOINT:-http://keycloak:8080/realms/auth-sandbox/protocol/openid-connect/token/introspect}
      KEYCLOAK_CMS_AUTH_PUBLIC_ENDPOINT: ${KEYCLOAK_CMS_AUTH_PUBLIC_ENDPOINT:-https://keycloak.localhost:8443/realms/auth-sandbox/protocol/openid-connect/auth}
      KEYCLOAK_CMS_TOKEN_ENDPOINT: ${KEYCLOAK_CMS_TOKEN_ENDPOINT:-http://keycloak:8080/realms/auth-sandbox/protocol/openid-connect/token}
    volumes:
      - ./auth-service/keys:/etc/auth-service/keys:ro
    depends_on:
      postgres:
        condition: service_healthy

  caddy:
    image: caddy:alpine
    restart: unless-stopped
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./app-mock-react/dist:/srv/app-mock:ro
      - ./admin-mock-react/dist:/srv/admin-mock:ro
      - ./home:/srv/home:ro
      - ./target-app-react/dist:/srv/target-app:ro
      - ./cms-content:/srv/cms-content:ro
      - ./cms-admin-react/dist:/srv/cms-admin:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - keycloak
      - auth-service

volumes:
  keycloak-postgres-data:
  caddy-data:
  caddy-config:
