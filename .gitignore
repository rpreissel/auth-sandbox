# Secrets / environment files
.env
tofu/terraform.tfvars
tofu/.terraform/
tofu/.terraform.lock.hcl
tofu/terraform.tfstate
tofu/terraform.tfstate.backup
tofu/terraform.tfstate.*.backup

# JWT key pairs (generated per environment — never commit)
device-login/keys/

# Caddy TLS data (legacy — replaced by nginx)
# caddy-data/

# nginx TLS certificates (generated by generate-certs.sh)
certs/

# Build artefacts
*.class
*.jar
target/
build/
bin/

# Gradle wrapper cache
.gradle/

# IDE files
.classpath
.project
.factorypath
.settings/

# Node / frontend
node_modules/
